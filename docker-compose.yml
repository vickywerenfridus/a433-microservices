version: "3.7"

services:
   item-db:
      image: mongo:3
      volumes:
         - app-db:/data/db
      restart: on-failure

   item-app:
      image: cungkriink/item-app:v1
      ports:
         - 80:8080
      restart: on-failure
      depends_on:
         - item-db
volumes:
   app-db:
